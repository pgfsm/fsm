// Swapped for ./fsmscheduler-invocation.node.ts in the npm/npx build — see
// fsmlet-invocation.ts for why runtime detection isn't used instead. This
// is the Deno-native default.
export const CLI_INVOCATION = "deno run --allow-all src/cli/fsmscheduler.ts";
